// src/test/setup.ts
// Runs once before the test suite. Adds jest-dom matchers (`toBeInTheDocument`,
// etc.) and centralizes any global test setup. Keep this file side-effect-only;
// per-test mocks belong in the test files.
import "@testing-library/jest-dom/vitest";
