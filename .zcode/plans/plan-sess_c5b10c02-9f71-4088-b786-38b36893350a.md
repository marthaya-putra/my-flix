# MyFlix → Apple-grade Makeover

**Direction:** refined red on true black · add `motion` for spring physics · dark-only perfected.

## Phase 0 — Foundations
- `npm i motion` (import: `motion/react`)
- Rework `src/styles/app.css`: OLED black bg, refined red accent, glass material tokens, optical typography, elevation shadows, `.glass` utility, scroll-edge fade `.glass-edge`

## Phase 1 — Core Surfaces
- **Navbar**: glass on scroll, spring-animated active pill via `layoutId`, transparent at top, scroll-edge fade
- **Root**: footer glass/edge, spacing tweaks
- **Not-found**: spring entrance, refined red

## Phase 2 — Poster Cards
- **movie-card**: spring press (`whileTap`), coordinated overlay (opacity + translateY spring), glass rating badge, vivid poster (no brightness dim)
- **recommendation-card**: same spring language, glass badges, spring expand materialization
- **card.tsx**: align badge/press to same family
- **Skeletons**: tune shimmer to new tokens

## Phase 3 — Carousels + Pagination
- Carousel buttons → glass + spring
- Unify the two near-identical `carouselButtonClassName` in content-row + rec-carousel
- Pagination → glass counter, spring press

## Phase 4 — Polish
- **Hero**: drop uppercase, optical title, spring entrance cascade, true-black gradient stops
- **button/badge/sonner**: finalize in-progress edits against new tokens

## Phase 5 — Verify
- `npm run typecheck`, click through all routes, verify springs + reduced-motion

**Not doing:** light theme, carousel physics rewrite (Embla is good), new fonts, layout/architecture changes.